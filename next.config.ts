/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['seus-domínios-de-imagem-aqui.com'],
  },
  // Adicione outras configurações necessárias
}

module.exports = nextConfig